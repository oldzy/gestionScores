<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kamal.gestionscores">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuActivity"
            android:label="@string/ecran_acceuil" />
        <activity
            android:name=".AddScoreActivity"
            android:label="@string/ecran_ajout_score" />
        <activity
            android:name=".Top10Activity"
            android:label="@string/ecran_top10" />
        <activity
            android:name=".ListeJeuxActivity"
            android:label="@string/ecran_liste_jeu" />
        <activity
            android:name=".ListePseudoActivity"
            android:label="@string/ecran_utilisateur" />
        <activity
            android:name=".WizardActivity"
            android:label="@string/ecran_wizard" />
    </application>

</manifest>